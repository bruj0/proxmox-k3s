# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# Repo-local build output
build/
logs/
*.audit.jsonl
bootstrap_state.json

# Secrets
.env
.env.*
terraform.tfvars
terraform.tfvars.*
output.json
k3s.json
# Operator-side port-forward kubeconfig (contains CA + token from the live CP)
kubeconfig.pveproxy
.pveproxy.state.json
.pveproxy.state.json.tmp

# OpenTofu (none in this repo, but the cluster-root directory
# is `infra/clusters/<name>` and inherits this for safety)
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
*.tfplan
*.tfplan.json

# SSH
known_hosts
*.ppk